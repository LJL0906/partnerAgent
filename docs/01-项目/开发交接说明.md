# 紫灵AI 开发交接说明

> 更新时间：2026 年 9 月 5 日
> 当前状态：**技术底座、聊天会话连续性和正式用户名密码登录已实现；当前优先完成聊天功能与 Android Expo Go 验收，业务保持 P1-01 完成、P1-02 暂缓**。键盘聚焦闪烁已做针对性修复但尚未真机确认；正式 Action 候选、`GetAnalysisRun` handler、确认中心页面、执行页和撤销业务闭环仍未实现。
>
> **本文件是唯一的交接说明。** 任务按依赖顺序推进，具体实现优先级见本文档「下一步优先级」一节。
>
> **2026-09-05 路线调整：** 用户决定先把聊天做成日常可用产品，再验证结构化提案，最后接正式业务。原定 9 月 6 日恢复 P1-02 的安排取消；P1-02 暂缓且无恢复日期。当前批次为 [聊天会话连续性](../05-任务架构/2026-09-05-聊天会话连续性.md)。

## 0. 新 Codex 会话接管摘要

新会话先读取本文件、根 `AGENTS.md`、根 `README.md`，再读取 [聊天会话连续性](../05-任务架构/2026-09-05-聊天会话连续性.md)、[用户名密码登录闭环](../05-任务架构/2026-09-05-用户名密码登录闭环.md) 和 [本轮集成交付](../05-任务架构/2026-09-05-聊天与账户集成交付.md)。前端修改前还须读取其 `AGENTS.md`。不要从旧聊天摘要推断当前状态；恢复业务时再读原 Action 方案。

截至本次交接：

- 用户已授权将全部当前代码与必要文档提交 Git；本轮基于 `main@4cbe9d7` 集成交付，最终提交请运行 `git log -1 --oneline` 查询，不在同一提交中写入自身哈希；后续修改仍需尊重用户授权范围；
- 当前 16 条迁移；正式账户注册、登录、刷新、退出已完成。历史会话、新建、切换、重启恢复和贴底规则已实现，前端功能优先于样式优化；
- 下一位开发者第一步是 Android 真机 / Expo Go 回归：聚焦与收起键盘、连续输入、默认贴底、上滑暂停、再次触底恢复、切换会话与冷启动登录恢复。Web 验证不能代替此项；
- 本轮 build、lint 和 449 项单元测试通过；memory e2e 121 项通过，20 项真实库测试未在本轮运行。账户独立验收记录包含 16 条迁移 up/down/up 和 8 项真实库测试；

以下为技术底座收口时的历史现场，迁移数量、进程和测试数量以最新记录为准：

- 阶段 0 曾以 `main` 分支、HEAD `0899d14` 和 88 项未提交工作树成果进入交接；这是历史现场，不代表当前 HEAD 或工作树数量；
- 阶段 0 成果已经形成可追溯基线提交 `2bdd9ce`；P1-01 的实现与验证在该基线之后完成，本交接不记录尚未知的最终功能提交哈希；
- 接管后必须重新检查当前分支、HEAD 和工作树，现有改动均不得 reset、checkout、覆盖或按“脏文件”清理；在用户明确要求前不要擅自 commit；
- 本机 Docker 容器 `partner-agent-postgres` 使用 PostgreSQL 16，宿主机端口为 `55433`；当前 15 条 migration 已在独立空库完成全量 up → down → up，后端单元测试 58 个文件、323/323 通过，前端单元测试 13 个文件、87/87 通过，后端全量 e2e 15 个文件、131/131 通过；临时验收库和角色已在本次收口后删除，业务库未修改；
- 本地忽略文件 `apps/partner-agent-backend/.env` 和 `apps/partner-agent-frontend/.env.local` 已用于联调，禁止读取后输出其中的密钥或提交它们；
- 本次现场后端曾监听 `0.0.0.0:3000`，Expo Metro 曾监听 `192.168.1.5:8081`。进程状态和 LAN IP 不具备跨会话稳定性，接管后必须重新检查，不能假设仍在运行；
- 用户已确认 Expo 连接成功。真实协议冒烟已验证 `user:self` ACK、REST `202`、session/task/operation 三频道 ACK、TaskStatus 和 ChatSession 对账 `200`。

P1-01 已完成，P1-02 按用户要求暂停且无恢复日期。后端技术底座已收口；会话列表、历史续聊、客户端恢复和账户已实现，下一步先验收原生聊天，再完善草稿、失败处理等日常功能，之后验证结构化提案。Prometheus 抓取/告警和备份恢复演练属于部署阶段；按个人单实例使用，不安排双实例故障注入或容量压测。

## 1. 项目边界

紫灵AI 是个人长期信息管理与行动辅助产品。当前代码已经完成可持久化聊天和阶段 0 安全可靠性基础，但尚未形成 Action 候选、确认、正式写入、执行和撤销的产品闭环；也不代表事实、目标、提醒或长期记忆纵切已经实现。

必须遵守以下边界：

- `pi/` 仅允许作为本地参考仓库，不纳入本仓库，也不修改后提交；
- 产品代码只通过 npm 包使用 Pi；
- `.env`、API Key、本地数据和构建产物不得提交；
- Pi 内部事件必须转换为产品契约后才能发送给前端；
- RAG 和 Agent 只能提供依据或候选，不能绕过确认中心写入正式事实、目标、行动或长期记忆。

当前检出目录中没有实际 `pi/`，但根 `.gitignore` 已保留 `/pi/` 保护规则。

## 2. 当前目录

```text
partnerAgent/
├── apps/
│   ├── partner-agent-backend/     # NestJS、Socket.IO、Pi Agent Runtime、Local Core
│   └── partner-agent-frontend/    # Expo / React Native（App Shell、聊天、鉴权、隐私决定）
├── packages/
│   └── contracts/                 # REST Command/Query 与 WS v1 共享契约
├── services/                      # 预留 Python AI/RAG 服务
├── docs/                          # 文档中心（详见 docs/README.md）
│   ├── 01-项目/                  # 项目边界、交接说明、P0 基线、阶段总结
│   ├── 02-需求/                  # 产品需求、技术架构与选型
│   ├── 03-技术草案/              # Local Core 路由/存储草案、Pi 分析
│   ├── 04-设计/                  # 前端设计参考图
│   └── 05-任务架构/              # 一次性任务架构与交付说明
├── package.json                   # npm workspaces 根配置
├── package-lock.json              # Workspace 唯一安装入口锁文件
├── README.md
└── AGENTS.md
```

Workspace 构建顺序是 `packages/* → apps/* → services/*`。contracts 必须先生成 `dist`，后端才能解析其声明文件。前端已由 `apps/*` 纳入 workspace。

## 3. 开发与验证

要求 Node.js 版本满足当前 NestJS、Vitest 和 Pi 包的约束。统一从仓库根目录安装：

```powershell
cd E:\project\partnerAgent
npm install
npm run build
npm run test
npm run lint
```

后端 e2e 单独执行：

```powershell
cd .\apps\partner-agent-backend
npm run test:e2e
```

启动生产构建：

```powershell
npm run build
cd .\apps\partner-agent-backend
npm run start:prod
```

默认 HTTP 与 Socket.IO 共用 `127.0.0.1:3000`，可用 `HOST` 和 `PORT` 覆盖；`PORT` 必须是 1 到 65535 的整数。真机联调需显式配置 `HOST=0.0.0.0`，并让 Expo 指向开发机的 LAN IP。浏览器和携带 Origin 的 WebSocket 请求必须命中 `CORS_ALLOWED_ORIGINS` 明确白名单；原生 Socket.IO 客户端通常不带 Origin，可以进入握手，但仍必须通过同一 HS256 JWT 校验。缺失或非法 JWT 仍会被拒绝。

## 4. 环境变量

正式账户启动顺序：构建 → 执行 16 条 migration 中尚未应用的迁移 → 启动后端 → 页面注册/登录。详见 [infra README](../../infra/README.md)。本地 HTTP Compose 必须叠加 `infra/compose.local.yml`；生产保持 HTTPS 与 Secure Cookie。原生 `EXPO_PUBLIC_SERVER_URL` 使用开发机 LAN IP，Web 单独配置 `EXPO_PUBLIC_WEB_SERVER_URL=http://localhost:3000`，页面使用 localhost 且 CORS 包含对应 8081 来源。不要让 Android 手机连接 localhost；更换网络后更新地址并重启 Metro。

本地文件位置：

```text
apps/partner-agent-backend/.env
```

DeepSeek 最小配置：

```dotenv
DEEPSEEK_API_KEY=<本地密钥>
DEFAULT_PROVIDER=deepseek
AUTH_JWT_SECRET=<至少 32 字节的随机密钥>
```

可选配置：

```dotenv
DEFAULT_MODEL=<模型 ID>
OPENAI_API_KEY=<本地密钥>
ANTHROPIC_API_KEY=<本地密钥>
PORT=3000
HOST=127.0.0.1              # 真机联调时显式改为 0.0.0.0
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8081,http://127.0.0.1:8081
MAX_SESSIONS_PER_USER=100
SESSION_STORE=postgres            # memory | postgres，决定会话存储方式
DATABASE_URL=postgresql://partner_agent:change-me@localhost:5432/partner_agent
DATABASE_POOL_SIZE=10
```

完整无密钥模板见 `apps/partner-agent-backend/.env.example`，其中还包含 ChatTask lease/polling、隐私决定、工具审批 TTL、模型超时/重试，以及 Agent deadline/轮次/工具/token 预算。启动时会统一校验和规范化这些配置；生产环境强制 PostgreSQL、非本地明确 CORS 和当前默认 Provider 对应的 API Key，错误只返回配置键名。

`CORS_ALLOWED_ORIGINS` 禁止使用 `*`，也不得无条件放开动态开发端口；Expo Web 常见来源 `http://localhost:8081` 与 `http://127.0.0.1:8081` 需逐项列入。不要记录、打印或提交真实值。`WS_PORT` 当前未被程序读取。旧 Agent WebSocket 已移出默认模块图；只有开发环境显式设置 `ENABLE_LEGACY_AGENT_WS=true` 才装载兼容模块，生产环境在配置校验阶段拒绝启用。

正式产品使用 `/api/v1/auth/register|login|refresh|logout` 和 `/api/v1/auth/me`；注册/登录经过凭据校验后签发令牌。旧开发 JWT CLI 仅保留为工程验证工具，产品已移除令牌输入界面。CLI 读取现有 `AUTH_JWT_SECRET`，要求明确 subject 和有限有效期，只输出令牌、不输出 secret：

```powershell
cd E:\project\partnerAgent\apps\partner-agent-backend
npm run dev:jwt -- --subject local-device-user --expires-in 900
```

将令牌作为 REST Bearer 或 Socket.IO `handshake.auth.token` 使用；不要放入 URL、Git、日志或截图。该 CLI 仅服务本地联调，不降低生产鉴权。

## 5. 共享契约

包名：`@partner-agent/contracts`

对外 v1 契约：

| 方向            | 事件                    | 用途                                                   |
| --------------- | ----------------------- | ------------------------------------------------------ |
| 客户端 → 服务端 | `/api/v1/*`             | 提交 Command 和 Query，共 36/38 个路由                 |
| 客户端 → 服务端 | `subscribe/unsubscribe` | 订阅 `task/operation/session/user:self` 频道           |
| 服务端 → 客户端 | `agent_event`           | 推送 `ServerPushEventV1`，使用 `snake_case/event_type` |

旧 `chat/cancel/resume_session` 是 deprecated 兼容通道，新客户端不再依赖它们。文字输入通过 `POST /api/v1/inputs/text`，任务取消通过 `POST /api/v1/tasks/cancel`，会话读取通过 `GET /api/v1/chat-sessions/:sessionId`。

会话列表通过 `GET /api/v1/chat-sessions` 返回当前 owner 的摘要，按最近活动倒序排列；列表和详情提供 `active_task` / `latest_task`（任务、操作 ID 与状态），用于打开历史后恢复进行中的回复。客户端只按服务地址与用户隔离保存所选会话 ID，正文仍从 REST 读取；退出清除所选引用，历史仍保存在服务端。当前读取范围仍受最近 100 条消息保留策略限制。

Command body 使用：

```json
{
  "operation_id": "UUID",
  "client_source": "web",
  "request_fingerprint": "sha256:...",
  "payload": { "text": "你好", "input_id": "input-123" }
}
```

客户端通过 HTTP Bearer 或 Socket.IO `handshake.auth.token` 发送 HS256 JWT。服务端只使用 JWT `sub` 作为可信用户标识，并要求令牌包含 `exp`；会剥离 body/query 中的 `user_id/userId`。

任务取消也使用 Command 信封：

```json
{
  "operation_id": "UUID",
  "client_source": "web",
  "request_fingerprint": "sha256:...",
  "payload": { "task_id": "task-123" }
}
```

服务端 v1 事件均包含：

```json
{
  "schema_version": 1,
  "event_id": "event-123",
  "channel": "task:task-123",
  "sequence": 1,
  "event_type": "text_delta",
  "session_id": "session-123",
  "timestamp": 1788494400000,
  "data": "文本增量"
}
```

详细频道和事件联合类见 `packages/contracts/src/events.ts`。

## 6. 会话模型

`SessionManager` 通过 `SESSION_STORE=memory|postgres` 选择进程内存或 PostgreSQL：

- 每个用户最多保存 `MAX_SESSIONS_PER_USER` 个会话，默认 100；不同用户的限额互不影响；
- 每个会话最多保存最近 100 条 user/assistant 消息；
- 已存在会话只能由 JWT `sub` 对应的 owner 访问，跨用户访问统一按“会话不存在”拒绝；
- 每个正式 ChatTask 使用已提交的 session 消息快照创建独立 Agent 实例，后入队消息不会污染当前任务上下文；
- PostgreSQL 模式通过原子认领、lease/fencing 和创建顺序保证同一 `sessionId` 严格串行，旧 worker 的晚到写入不能覆盖新 fence；
- 同一 Socket 可以同时管理不同会话；
- 正式 v1 取消按 `taskId` 和 owner 生效，不以 Socket 连接作为任务身份；
- Socket 断开不取消已受理任务；
- LISTEN 重连后按本机已投递 position 对活跃 stream 主动 catch-up；客户端重连仍可按持久频道水位重放，游标失效时通过任务和会话 REST 查询恢复；事件同时受 count 与 age 配置约束并由后台分批清理；
- 新会话首次连接只订阅 `user:self`；在 REST 创建会话并返回权威引用后，才增量订阅 session/task/operation，并立即读取 TaskStatus 与 ChatSession 对账；
- 不允许为绕过首次订阅失败而放宽后端对不存在 session 的所有权校验，否则可能产生跨用户预订阅泄露。

限制：内存模式在服务重启后会丢失；数据库模式可以恢复 queued/running、隐私等待和工具审批等待任务，但网络分区下 Provider 调用本身仍不保证 exactly-once；当前只有完整回合裁剪、没有摘要压缩。正式账户不自动合并旧开发身份数据，找回/修改密码、验证码和 MFA 尚未实现。LISTEN 重连 catch-up 只覆盖本实例活跃订阅，游标超过保留窗口仍返回 `recovery_required`，REST 始终是权威恢复源。

## 7. 工具模型

已接入只读工具：

```text
get_current_time
```

该工具无输入参数，返回服务器 ISO 8601 时间。当前权限与边界：

- 无副作用只读工具允许自动执行；
- 自动只读工具结果只进入当前 Agent 回合；需要批准的外部工具会持久化脱敏结果快照，以支持批准/拒绝后重启续推；
- 模型只能看到工具公开名称、描述、参数 Schema 和筛选后的结果；
- 前端工具事件只包含工具名、调用 ID 与成功状态；
- 不向前端透传 Pi 原始 result、参数、堆栈、密钥或环境变量；
- `tool-registry` 按 `riskLevel`(read_only/low/medium/high) 分级；`high` 必须 `requiresConfirmation`；
- **正式业务工具只生成 Candidate/Batch，不直接写正式对象**；正式对象（Goal/Action/Fact/Memory/Decision/Situation/Reminder）只能经 `SubmitConfirmationBatch` 生效；
- 外部系统副作用用独立的 **Tool Approval**（`confirm_tool_execution`/`dismiss_tool_execution`/`undo_tool_execution`），与正式 `ConfirmationBatch` 严格区分；
- 外部副作用执行中进程崩溃会进入 `indeterminate`，系统不自动重放；运维人员使用 `npm run tool:reconcile --workspace @partner-agent/backend -- ...` 在本机按固定结论核对并写脱敏审计，原失败任务不恢复；
- 审计记录 requester、session、tool、operation/call ID、脱敏摘要、结果和时间；
- 值级脱敏覆盖 Bearer/JWT/API Key、内部路径和异常文本，不只按字段名脱敏。

测试客户端位于：

```text
apps/partner-agent-backend/test-client.html
```

从 `CORS_ALLOWED_ORIGINS` 中的 HTTP 来源打开页面，填写上述 CLI 生成的有效 JWT 后连接；随后可编辑会话 ID。Expo Web 默认开发端口通常为 `8081`，应按实际使用的 `localhost` 或 `127.0.0.1` 来源逐项配置，不允许通配端口。输入“现在几点？”可验证工具开始、工具完成和最终模型回复。不要把令牌放进 URL、Git 或截图。

## 8. 主要代码

```text
apps/partner-agent-backend/src/
├── local-core-api/                    # /api/v1 REST 路由、鉴权、应用端口与确认批次事务
├── database/
│   ├── entities/core/                 # Local Core 正式对象实体（用户/正式对象/确认/历史）
│   ├── migrations/                    # 当前 16 条可逆迁移，新增账户凭据与刷新会话
│   ├── database-definition.ts         # Entity/Migration 唯一注册表
│   ├── data-source.ts                 # TypeORM CLI DataSource
│   └── typeorm-session.store.ts       # PostgreSQL 会话存储
├── health/                            # live/ready、migration readiness 与 draining
├── ws-v1/                             # /ws/v1 授权、工具控制、持久事件、重放与恢复
├── tools/                             # 工具注册表 + 值级脱敏 + 外部 Tool Approval
├── agent/
│   ├── agent.gateway.ts               # 旧 WebSocket 编排与事件转换（deprecated 兼容）
│   ├── pi-agent.service.ts            # 每任务 Agent 生命周期、恢复和工具注册
│   ├── agent-runtime-policy.ts        # deadline/轮次/工具/token 预算
│   ├── agent-runtime-telemetry.ts     # 无正文的关联遥测
│   ├── session-manager.service.ts     # 会话管理（memory/postgres 可选择）
│   └── tools/current-time.tool.ts     # 只读时间工具
├── config/runtime-config.ts           # 启动配置统一校验
├── auth/                              # 用户名密码账户、刷新/撤销、JWT HS256 与 WS 握手守卫
├── websocket/secure-io.adapter.ts     # CORS 白名单 + JWT WS 适配器
└── model-gateway/
    └── model-gateway.service.ts       # Provider 与模型目录

packages/contracts/src/
├── events.ts                          # WS v1 订阅/推送契约、含安全 candidate 资源引用
├── local-core.ts                      # 基础信封、Command、共享状态基线（463 行）
├── local-core-analysis.ts             # 分析类型、Action DTO、AnalysisTaskRef
├── local-core-model.ts                # 模型配置契约
├── local-core-queries.ts              # Query DTO；GetAnalysisRun 仅冻结契约
└── index.ts
```

## 9. 当前验证基线

本轮集成重新执行 build、lint、单元和 memory e2e，结果见 [集成交付记录](../05-任务架构/2026-09-05-聊天与账户集成交付.md)：contracts 12、backend 325、frontend 112；memory e2e 121。真实库本轮跳过 20 项；账户阶段已验证 16 条迁移和 8 项真实库账户测试。Android/iOS 真机未完成验收。

以下是账户与会话批次之前的技术底座历史基线，不代表当前计数或本轮重跑结果：

```text
npm run build                    通过（contracts + backend + frontend）
npm run test                     通过（contracts 12/12；backend 323/323；frontend 87/87，共 422 项）
contracts Jest                   2 个套件、12/12 通过
backend Vitest                   58 个文件、323/323 通过
frontend Vitest                  13 个文件、87/87 通过
npm run test:e2e（memory）       7 个文件、112/112 通过
npm run lint                     通过
PostgreSQL 16 migration         当前 15 条全量 up → down → up 通过
后端全量 e2e                    15 个文件、131/131 通过
真实 HTTP/WS 冒烟               user:self ACK → REST 202 → 动态三频道 ACK → 双 REST 200
Expo 设备连接                   用户确认通过
真实 DeepSeek                   完整外发决策、调用、持久化与 trace 通过
生产镜像 / 单实例 Compose        构建、迁移、健康、重启、持久化通过
```

真实 PostgreSQL 验证使用独立可丢弃空库；覆盖确认事务、聊天与重启、任务 worker/通知事务可见性、外发审计、隐私恢复、分析拒绝、ChatTask lease、WS 事件、工具控制 Outbox 与毒性事件处置。2026 年 9 月 5 日已通过真实 DeepSeek 端到端，并实际构建 `partner-agent-backend:local`；隔离 Compose 项目的 PostgreSQL、migration 和 backend 已完成迁移、健康、重启和持久化验证。验证记录未保存密钥、JWT、数据库口令或完整请求载荷；详见 [`Agent 底座收口验证记录`](../05-任务架构/2026-09-05-Agent底座收口验证记录.md)。多实例故障注入按个人单实例使用决策不纳入验收。

后续安全加固已将递归对象/数组/字段扫描、递归脱敏与复扫、循环/深度/节点/字符串上限 fail-closed 以及可等待无明文审计落地；审计写入失败时 Provider 零调用，任务以 `EGRESS_001` 安全失败。`SubmitTextInput` 的分析参数也已改为显式校验：尚未接通的合法分析请求同步返回 `NOT_IMPLEMENTED_001`，不再降级为普通聊天，只保留最小幂等操作记录。

阶段 0 已关闭，首次订阅保持 `user:self → REST 创建资源 → 动态频道 ACK → REST 对账`。当前正式账户访问令牌仅内存保存，最长 15 分钟；Native 刷新凭据使用 SecureStore，Web 使用 HttpOnly SameSite=Strict Cookie，刷新期限为 7 天且原子轮换。退出撤销当前会话；当前限速和 WS 撤销通知按个人单实例设计。真实供应商长期稳定性与完整原生流程仍需持续验证，P1-02 不自动恢复。

P1-01 已完成以下基础：`ANALYSIS_TYPES` 纳入 `action`；Action DTO、`AnalysisTaskRef` 与 WS `candidate` 安全资源引用冻结；`local-core.ts` 拆至 463 行并形成 analysis/model/queries 三个稳定分层；新增 `analysis_runs`、`structured_analyses` 实体和第 8 条 migration。以上只代表契约与持久化基础完成，不代表候选生产、`GetAnalysisRun` handler 或前端页面已经实现。

工具测试覆盖完整 Agent 两轮循环：模型提出工具调用，真实时间工具执行，工具结果回到模型上下文，模型生成最终文本，前端过程事件同步产生。

## 10. 下一步优先级

> 当前技术加固的完成项和边界见 [Agent 技术底座加固](../05-任务架构/2026-09-04-Agent技术底座加固.md)；多实例路线见 [多实例任务执行与事件分发](../03-技术草案/多实例任务执行与事件分发.md)。
> 剩余纯技术研发的可分派任务、文件所有权和完成条件见 [Agent 技术底座剩余研发交接](../05-任务架构/2026-09-04-Agent技术底座剩余研发交接.md)。

1. 首先完成 Android Expo Go 真机验收：登录/刷新/退出，输入聚焦与键盘收放不闪烁，消息增长默认贴底，上滑不被拉回，触底继续跟随，历史切换和冷启动不串线。修复涉及 `chat-screen.tsx`、`chat-input.tsx` 和 `消息贴底策略.ts`；如仍复现，记录系统、键盘版本、触发步骤及布局高度，按最小范围修复，不把 Web 通过当作真机通过。
2. 然后逐项核对聊天功能缺口，优先草稿保留、发送失败与恢复提示、长消息阅读等实际交互；账户与会话基础不重复开发，样式优化后置。完成条件是核心聊天流程可日常使用并补上对应验证记录。
3. 聊天稳定后验证经服务端校验、来源可追溯的结构化提案；分析产物不等于正式 Action，不绕过确认事务。
4. **P1-02 暂缓且无恢复日期**。正式业务恢复时重新核对原 Action 方案；提案工具、候选事务、查询/撤销与前端页面按依赖逐项推进，不提前宣称完成。
5. 后续再评审 Goal/Memory、摘要和 Python AI/RAG；Redis/BullMQ、LangGraph 和 pgvector 不是当前预置组件。
6. 部署侧后续补 Prometheus 抓取/告警和备份恢复演练；个人单实例使用不安排双实例故障注入或容量压测。

> 所有任务遵循统一纪律：正式业务数据只能由 `SubmitConfirmationBatch` 事务写入；新增写能力前必须具备确认、授权、审计、撤销四要素；不修改 `pi/`；不提交 `.env` 与密钥。

## 11. 提交前检查

- `npm run build`、`npm run test`、`npm run lint` 全部通过；
- 后端 Local Core REST、旧 Socket.IO 兼容层与 `/ws/v1` e2e 全部通过；
- `.env` 和 API Key 未进入 Git；
- `pi/` 未进入主仓库；
- WebSocket 事件没有直接暴露 Pi 内部结构；
- 新增写能力时必须有确认、授权、审计和撤销设计。
- 新增数据库表具有 owner 约束、索引和 down migration，并完成真实 PostgreSQL up → down → up；
- 复审根 README、本交接说明、任务架构和 P0/路由文档，保证状态与实现一致；
- 新增或拆分后的单个源码文件原则上不超过 500 行。
