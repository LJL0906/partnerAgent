# 紫灵AI 开发交接说明

> 更新时间：2026 年 9 月 4 日
> 当前状态：**阶段 0 可靠性与安全门槛已关闭，主线进入阶段 1「Action 候选生产」**。P0 决策、共享契约、REST 文字输入/任务主链路、隐私外发决定、外发审计 fail-closed、递归敏感扫描、分析参数显式校验、确认批次事务、WS v1 权威订阅、Expo 统一 token provider 与真机 HTTP/WS 连接均已落地；正式 Action 候选生产、确认中心、执行页和撤销闭环尚未实现。
>
> **本文件是唯一的交接说明。** 任务按依赖顺序推进，具体实现优先级见本文档「下一步优先级」一节。

## 0. 新 Codex 会话接管摘要

新会话先读取本文件、根 `AGENTS.md`、根 `README.md` 和 [`行动候选与确认中心首个业务纵切`](../05-任务架构/2026-09-04-行动候选与确认中心首个业务纵切.md)，不要从旧聊天摘要推断当前状态。

截至本次交接：

- Git 分支为 `main`，当前 HEAD 为 `0899d14`；
- 工作树包含阶段 0 多轮任务的未提交成果：53 个已跟踪文件修改、35 个未跟踪路径，共 88 项；这些都是当前项目成果，不得 reset、checkout、覆盖或按“脏文件”清理；
- 在用户明确要求提交前，不要擅自 commit；继续多人协作前，应先由一个主会话审阅并固化唯一阶段 0 基线，避免共享 Contracts 和 migration 产生交叉覆盖；
- 本机 Docker 容器 `partner-agent-postgres` 使用 PostgreSQL 16，宿主机端口为 `55433`；当前数据库已经应用全部 7 条 migration；
- 本地忽略文件 `apps/partner-agent-backend/.env` 和 `apps/partner-agent-frontend/.env.local` 已用于联调，禁止读取后输出其中的密钥或提交它们；
- 本次现场后端曾监听 `0.0.0.0:3000`，Expo Metro 曾监听 `192.168.1.5:8081`。进程状态和 LAN IP 不具备跨会话稳定性，接管后必须重新检查，不能假设仍在运行；
- 用户已确认 Expo 连接成功。真实协议冒烟已验证 `user:self` ACK、REST `202`、session/task/operation 三频道 ACK、TaskStatus 和 ChatSession 对账 `200`。

接管后的第一项业务任务是本文第 10 节的 **P1-01：Action 分析契约与数据基础**。不要跳到前端候选卡，也不要同时扩展 Goal、Fact、Memory、Reminder 或 RAG。

## 1. 项目边界

紫灵AI 是个人长期信息管理与行动辅助产品。当前代码已经完成可持久化聊天和阶段 0 安全可靠性基础，但尚未形成 Action 候选、确认、正式写入、执行和撤销的产品闭环；也不代表事实、目标、提醒或长期记忆纵切已经实现。

必须遵守以下边界：

- `pi/` 仅允许作为本地参考仓库，不纳入本仓库，也不修改后提交；
- 产品代码只通过 npm 包使用 Pi；
- `.env`、API Key、本地数据和构建产物不得提交；
- Pi 内部事件必须转换为产品契约后才能发送给前端；
- RAG 和 Agent 只能提供依据或候选，不能绕过确认中心写入正式事实、目标、行动或长期记忆。

当前检出目录中没有实际 `pi/`，但根 `.gitignore` 已保留 `/pi/` 保护规则。

## 2. 当前目录

```text
partnerAgent/
├── apps/
│   ├── partner-agent-backend/     # NestJS、Socket.IO、Pi Agent Runtime、Local Core
│   └── partner-agent-frontend/    # Expo / React Native（App Shell、聊天、鉴权、隐私决定）
├── packages/
│   └── contracts/                 # REST Command/Query 与 WS v1 共享契约
├── services/                      # 预留 Python AI/RAG 服务
├── docs/                          # 文档中心（详见 docs/README.md）
│   ├── 01-项目/                  # 项目边界、交接说明、P0 基线、阶段总结
│   ├── 02-需求/                  # 产品需求、技术架构与选型
│   ├── 03-技术草案/              # Local Core 路由/存储草案、Pi 分析
│   ├── 04-设计/                  # 前端设计参考图
│   └── 05-任务架构/              # 一次性任务架构与交付说明
├── package.json                   # npm workspaces 根配置
├── package-lock.json              # Workspace 唯一安装入口锁文件
├── README.md
└── AGENTS.md
```

Workspace 构建顺序是 `packages/* → apps/* → services/*`。contracts 必须先生成 `dist`，后端才能解析其声明文件。前端已由 `apps/*` 纳入 workspace。

## 3. 开发与验证

要求 Node.js 版本满足当前 NestJS、Vitest 和 Pi 包的约束。统一从仓库根目录安装：

```powershell
cd E:\project\partnerAgent
npm install
npm run build
npm run test
npm run lint
```

后端 e2e 单独执行：

```powershell
cd .\apps\partner-agent-backend
npm run test:e2e
```

启动生产构建：

```powershell
npm run build
cd .\apps\partner-agent-backend
npm run start:prod
```

默认 HTTP 与 Socket.IO 共用 `127.0.0.1:3000`，可用 `HOST` 和 `PORT` 覆盖；`PORT` 必须是 1 到 65535 的整数。真机联调需显式配置 `HOST=0.0.0.0`，并让 Expo 指向开发机的 LAN IP。浏览器和携带 Origin 的 WebSocket 请求必须命中 `CORS_ALLOWED_ORIGINS` 明确白名单；原生 Socket.IO 客户端通常不带 Origin，可以进入握手，但仍必须通过同一 HS256 JWT 校验。缺失或非法 JWT 仍会被拒绝。

## 4. 环境变量

本地文件位置：

```text
apps/partner-agent-backend/.env
```

DeepSeek 最小配置：

```dotenv
DEEPSEEK_API_KEY=<本地密钥>
DEFAULT_PROVIDER=deepseek
AUTH_JWT_SECRET=<至少 32 字节的随机密钥>
```

可选配置：

```dotenv
DEFAULT_MODEL=<模型 ID>
OPENAI_API_KEY=<本地密钥>
ANTHROPIC_API_KEY=<本地密钥>
PORT=3000
HOST=127.0.0.1              # 真机联调时显式改为 0.0.0.0
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8081,http://127.0.0.1:8081
MAX_SESSIONS_PER_USER=100
SESSION_STORE=postgres            # memory | postgres，决定会话存储方式
DATABASE_URL=postgresql://partner_agent:change-me@localhost:5432/partner_agent
DATABASE_POOL_SIZE=10
```

`CORS_ALLOWED_ORIGINS` 禁止使用 `*`，也不得无条件放开动态开发端口；Expo Web 常见来源 `http://localhost:8081` 与 `http://127.0.0.1:8081` 需逐项列入。不要记录、打印或提交真实值。`WS_PORT` 当前未被程序读取。

本地开发令牌通过 CLI 签发，不开放匿名 HTTP token 接口。CLI 读取现有 `AUTH_JWT_SECRET`，要求明确 subject 和有限有效期，只输出令牌、不输出 secret：

```powershell
cd E:\project\partnerAgent\apps\partner-agent-backend
npm run dev:jwt -- --subject local-device-user --expires-in 900
```

将令牌作为 REST Bearer 或 Socket.IO `handshake.auth.token` 使用；不要放入 URL、Git、日志或截图。该 CLI 仅服务本地联调，不降低生产鉴权。

## 5. 共享契约

包名：`@partner-agent/contracts`

对外 v1 契约：

| 方向 | 事件 | 用途 |
| --- | --- | --- |
| 客户端 → 服务端 | `/api/v1/*` | 提交 Command 和 Query，共 36/37 个路由 |
| 客户端 → 服务端 | `subscribe/unsubscribe` | 订阅 `task/operation/session/user:self` 频道 |
| 服务端 → 客户端 | `agent_event` | 推送 `ServerPushEventV1`，使用 `snake_case/event_type` |

旧 `chat/cancel/resume_session` 是 deprecated 兼容通道，新客户端不再依赖它们。文字输入通过 `POST /api/v1/inputs/text`，任务取消通过 `POST /api/v1/tasks/cancel`，会话读取通过 `GET /api/v1/chat-sessions/:sessionId`。

Command body 使用：

```json
{
  "operation_id": "UUID",
  "client_source": "web",
  "request_fingerprint": "sha256:...",
  "payload": { "text": "你好", "input_id": "input-123" }
}
```

客户端通过 HTTP Bearer 或 Socket.IO `handshake.auth.token` 发送 HS256 JWT。服务端只使用 JWT `sub` 作为可信用户标识，并要求令牌包含 `exp`；会剥离 body/query 中的 `user_id/userId`。

任务取消也使用 Command 信封：

```json
{
  "operation_id": "UUID",
  "client_source": "web",
  "request_fingerprint": "sha256:...",
  "payload": { "task_id": "task-123" }
}
```

服务端 v1 事件均包含：

```json
{
  "schema_version": 1,
  "event_id": "event-123",
  "channel": "task:task-123",
  "sequence": 1,
  "event_type": "text_delta",
  "session_id": "session-123",
  "timestamp": 1788494400000,
  "data": "文本增量"
}
```

详细频道和事件联合类见 `packages/contracts/src/events.ts`。

## 6. 会话模型

`SessionManager` 通过 `SESSION_STORE=memory|postgres` 选择进程内存或 PostgreSQL：

- 每个用户最多保存 `MAX_SESSIONS_PER_USER` 个会话，默认 100；不同用户的限额互不影响；
- 每个会话最多保存最近 100 条 user/assistant 消息；
- 已存在会话只能由 JWT `sub` 对应的 owner 访问，跨用户访问统一按“会话不存在”拒绝；
- Agent 实例按 `sessionId` 创建并复用；
- 单个 `sessionId` 同时只允许一个生成请求；
- 同一 Socket 可以同时管理不同会话；
- 正式 v1 取消按 `taskId` 和 owner 生效，不以 Socket 连接作为任务身份；
- Socket 断开不取消已受理任务；
- 重连后按频道水位重放，游标失效时通过任务和会话 REST 查询恢复。
- 新会话首次连接只订阅 `user:self`；在 REST 创建会话并返回权威引用后，才增量订阅 session/task/operation，并立即读取 TaskStatus 与 ChatSession 对账；
- 不允许为绕过首次订阅失败而放宽后端对不存在 session 的所有权校验，否则可能产生跨用户预订阅泄露。

限制：内存模式在服务重启后会丢失；数据库模式已使用消息 sequence 水位恢复完整 Agent snapshot，当前仍只有完整回合裁剪、没有摘要压缩；尚未提供正式登录或 HTTP 令牌签发接口，本地开发仅使用读取既有 secret 的短期 JWT CLI。

## 7. 工具模型

已接入只读工具：

```text
get_current_time
```

该工具无输入参数，返回服务器 ISO 8601 时间。当前权限与边界：

- 无副作用只读工具允许自动执行；
- 工具结果不落库，只进入当前 Agent 会话上下文；
- 模型只能看到工具公开名称、描述、参数 Schema 和筛选后的结果；
- 前端工具事件只包含工具名、调用 ID 与成功状态；
- 不向前端透传 Pi 原始 result、参数、堆栈、密钥或环境变量；
- `tool-registry` 按 `riskLevel`(read_only/low/medium/high) 分级；`high` 必须 `requiresConfirmation`；
- **正式业务工具只生成 Candidate/Batch，不直接写正式对象**；正式对象（Goal/Action/Fact/Memory/Decision/Situation/Reminder）只能经 `SubmitConfirmationBatch` 生效；
- 外部系统副作用用独立的 **Tool Approval**（`confirm_tool_execution`/`dismiss_tool_execution`/`undo_tool_execution`），与正式 `ConfirmationBatch` 严格区分；
- 审计记录 requester、session、tool、operation/call ID、脱敏摘要、结果和时间；
- 值级脱敏覆盖 Bearer/JWT/API Key、内部路径和异常文本，不只按字段名脱敏。

测试客户端位于：

```text
apps/partner-agent-backend/test-client.html
```

从 `CORS_ALLOWED_ORIGINS` 中的 HTTP 来源打开页面，填写上述 CLI 生成的有效 JWT 后连接；随后可编辑会话 ID。Expo Web 默认开发端口通常为 `8081`，应按实际使用的 `localhost` 或 `127.0.0.1` 来源逐项配置，不允许通配端口。输入“现在几点？”可验证工具开始、工具完成和最终模型回复。不要把令牌放进 URL、Git 或截图。

## 8. 主要代码

```text
apps/partner-agent-backend/src/
├── local-core-api/                    # /api/v1 REST 路由、鉴权、应用端口与确认批次事务
├── database/
│   ├── entities/core/                 # Local Core 正式对象实体（用户/正式对象/确认/历史）
│   ├── migrations/                    # 会话表、工具确认表、Local Core schema 三组迁移
│   ├── data-source.ts                 # TypeORM DataSource（migrations + 连接配置）
│   └── typeorm-session.store.ts       # PostgreSQL 会话存储
├── ws-v1/                             # /ws/v1 订阅、授权、顺序推送、重放与恢复
├── tools/                             # 工具注册表 + 值级脱敏 + 外部 Tool Approval（deprecated 兼容）
├── agent/
│   ├── agent.gateway.ts               # 旧 WebSocket 编排与事件转换（deprecated 兼容）
│   ├── pi-agent.service.ts            # Agent 生命周期、历史恢复和工具注册
│   ├── session-manager.service.ts     # 会话管理（memory/postgres 可选择）
│   └── tools/current-time.tool.ts     # 只读时间工具
├── auth/                              # JWT HS256 认证、WS 握手守卫
├── websocket/secure-io.adapter.ts     # CORS 白名单 + JWT WS 适配器
└── model-gateway/
    └── model-gateway.service.ts       # Provider 与模型目录

packages/contracts/src/
├── events.ts                          # WS v1 订阅/推送契约、ServerPushEventV1
├── local-core.ts                      # Command/Query、统一错误码、状态基线
└── index.ts
```

## 9. 当前验证基线

截至本次交接，以下结果均有实际命令或真实协议验证记录：

```text
npm run build                    通过（contracts + backend + frontend）
npm run test                     通过（contracts 8/8；backend 163/163；frontend 80/80，共 251 项）
contracts Jest                   2 个套件、8/8 通过
backend Vitest                   27 个文件、163/163 通过
frontend Vitest                  10 个文件、80/80 通过
npm run test:e2e                 6 个文件、107/107 通过，真实 PG 专项默认跳过
npm run lint                     通过
PostgreSQL 16                    7 个 migration up → down → up 通过
当前 Docker 数据库              migration:show 为 7/7 已应用
真实 PostgreSQL 专项            聊天/确认 2/2；重启恢复 1/1；外发审计 3/3；分析拒绝 1/1 通过
真实 HTTP/WS 冒烟               user:self ACK → REST 202 → 动态三频道 ACK → 双 REST 200
Expo 设备连接                   用户确认通过
```

真实 PostgreSQL 验证使用独立可丢弃的 PostgreSQL 16.15 容器；确认事务覆盖真实幂等、指纹冲突、同批并发、版本冲突和索引任务，数据库约束验证 24 小时期限、高风险单候选及 owner 复合外键，故障注入全量回滚由定向事务测试覆盖。真实 HTTP/Socket.IO 闭环使用确定性测试 Agent，未消耗或记录第三方模型凭证；实际供应商质量与网络行为仍需设备联调。

后续安全加固已将递归对象/数组/字段扫描、递归脱敏与复扫、循环/深度/节点/字符串上限 fail-closed 以及可等待无明文审计落地；审计写入失败时 Provider 零调用，任务以 `EGRESS_001` 安全失败。`SubmitTextInput` 的分析参数也已改为显式校验：尚未接通的合法分析请求同步返回 `NOT_IMPLEMENTED_001`，不再降级为普通聊天，只保留最小幂等操作记录。

阶段 0 已关闭：首次订阅采用 `user:self → REST 创建资源 → 动态频道 ACK → REST 对账`，HTTP 与 WS 共用同一安全 token provider；Native 使用 SecureStore，Web 仅内存保存；本地开发 JWT 由 CLI 短期签发。正式登录、刷新令牌和商业化账户体系仍不在当前范围。真实供应商的长期稳定性和完整 Expo 业务纵切仍是最终端到端验收项，不阻塞阶段 1 开工。

已知文档漂移：根 `README.md` 和业务纵切任务文档的部分状态注记仍写着“首次订阅/token 待完成”。本文件是当前交接权威；后续做阶段 0 基线收口时应同步修正，但不要因此扩大 P1-01 的业务实现范围。

工具测试覆盖完整 Agent 两轮循环：模型提出工具调用，真实时间工具执行，工具结果回到模型上下文，模型生成最终文本，前端过程事件同步产生。

## 10. 下一步优先级

> 当前一次性实施方案与队员任务拆分见 [行动候选与确认中心首个业务纵切](../05-任务架构/2026-09-04-行动候选与确认中心首个业务纵切.md)。任务文档只指导本次交付，不取代本文档的当前状态权威性。

1. 在用户授权提交后，先把当前阶段 0 成果形成一个可追溯的基线提交；提交前复核 88 项工作树变更，不得丢弃任何现有成果。
2. 执行 **P1-01：Action 分析契约与数据基础**：
   - 由单一 owner 修改并合并 `@partner-agent/contracts`，先把 `action` 加入权威 `ANALYSIS_TYPES`；当前合法值只有 `idea_organize`、`experience_review`、`problem_analysis`、`content_extract`，后端不得提前私自接受 `action`；
   - 为 Action 分析定义最小 DTO、任务引用、结构化提案、候选结果和 WS `candidate` 资源引用语义；
   - `packages/contracts/src/local-core.ts` 已超过建议长度，应按稳定导出边界拆分，不继续堆叠新 DTO，同时保持包入口兼容；
   - 新增最小 `analysis_runs`、`structured_analyses` 实体和可回滚 migration，并建立 owner、OriginalRecord、ChatTask 的复合所有权约束、状态约束和必要索引；
   - 本任务只冻结契约和持久化基础，不接前端页面，不把候选塞入 ChatTask 任意 JSON 字段。
3. P1-01 通过后执行 **P1-02：Action 候选生产链**：注册严格 TypeBox `propose_action_candidates` 提案工具，完成服务端 Schema/业务/来源/时间/风险校验，并在单事务内写入 AnalysisRun、StructuredAnalysis、ConfirmationBatch、CandidateItem 和 SourceRelation，提交后发布仅含资源引用的 `candidate` 事件。模型和 Pi 只能提案，不能写正式 Action。
4. 候选 DTO 稳定后并行推进：后端接通待确认批次、候选详情、确认历史、Action 列表/详情/来源/历史、撤销资格和整批撤销候选；Expo 接入聊天候选卡、确认中心和执行页。共享 Contracts 仍由一个 owner 统一合并。
5. 最后使用确定性 Agent、真实 PostgreSQL、真实 HTTP/Socket.IO 和 Expo 设备跑通“文字输入 → Action 候选 → 确认 → 正式 Action → 执行页 → 撤销候选 → 再确认”。业务纵切稳定后才能扩展 Goal/Memory、摘要和 Python AI/RAG，也不要提前移除 deprecated 通道。

> 所有任务遵循统一纪律：正式业务数据只能由 `SubmitConfirmationBatch` 事务写入；新增写能力前必须具备确认、授权、审计、撤销四要素；不修改 `pi/`；不提交 `.env` 与密钥。

## 11. 提交前检查

- `npm run build`、`npm run test`、`npm run lint` 全部通过；
- 后端 Local Core REST、旧 Socket.IO 兼容层与 `/ws/v1` e2e 全部通过；
- `.env` 和 API Key 未进入 Git；
- `pi/` 未进入主仓库；
- WebSocket 事件没有直接暴露 Pi 内部结构；
- 新增写能力时必须有确认、授权、审计和撤销设计。
- 新增数据库表具有 owner 约束、索引和 down migration，并完成真实 PostgreSQL up → down → up；
- 复审根 README、本交接说明、任务架构和 P0/路由文档，保证状态与实现一致；
- 新增或拆分后的单个源码文件原则上不超过 500 行。
