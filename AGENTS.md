# 协作规范

- 简单任务直接执行；仅在缺少关键信息或操作风险较高时确认。
- 开始前只读取当前任务适用的 `AGENTS.md`、`README.md` 和必要说明；规则冲突时，以目标文件最近的 `AGENTS.md` 为准。
- 采用“手术刀式修改”：只改完成当前任务所必需的代码和文件，不顺手重构、不扩大范围、不增加无关兼容层。
- 避免过度抽象；单个组件或源码文件尽量控制在 500 行以内。
- 不修改、覆盖或丢弃与当前任务无关的已有改动。
- 修改完成后执行与改动范围匹配的检查或测试，不得把未验证结果描述为已通过。
- 完成需求后复审任务范围内的相关 Markdown 文档，确认技术选型、需求实现、方案决策和项目进度没有跑偏；如有冲突，明确说明并给出纠正方案。
- 交付时列出新增、修改、删除的文件；没有文件变更时明确说明。
