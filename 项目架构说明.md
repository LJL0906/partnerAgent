# 项目架构说明

> 状态：基础运行架构完成，正式业务数据闭环暂缓
>
> 更新时间：2026 年 9 月 4 日

## 1. 项目定位

Partner Agent 是一个个人助手产品，目标是围绕个人输入、事实与长期记忆、行动、目标、确认中心、提醒和决策辅助，形成可追溯、可确认、可撤销的长期信息管理闭环。

当前阶段的重点不是增加业务功能，而是稳定以下基础边界：

- 产品需求与技术架构的对应关系；
- Local Core、NestJS、Pi Agent Runtime、Python AI/RAG 之间的职责；
- 数据库、文件存储、任务和索引的一致性边界；
- 前端、后端和 AI 服务之间的接口契约；
- 本地开发、私有部署和后续扩展的仓库组织方式。

## 2. Git 仓库策略

### 2.1 产品主仓库

当前目录 `partnerAgent` 是产品主仓库。以下内容应由主仓库跟踪：

- `需求/` 中的产品和技术文档；
- `apps/partner-agent-backend/` 中的 NestJS 业务后端；
- `packages/contracts/` 中的跨端共享契约；
- 后续创建的前端项目；
- 后续创建的 Python AI/RAG 项目；
- 接口契约、数据库迁移、部署文件和项目级脚本；
- 产品级 README、变更记录和开发约定。

### 2.2 Pi 参考仓库

`pi/` 是官方 Pi 项目的本地克隆，目的是方便 Agent 学习、阅读源码和进行技术分析。

它具有独立的 `.git` 和官方远程仓库，因此遵循以下规则：

- 不纳入 `partnerAgent` 主仓库；
- 不使用 Git Submodule；
- 不把 Pi 源码复制进产品目录提交；
- Pi 的 Git 操作只在 `pi/` 目录中进行；
- 对 Pi 的阅读结论可以整理到主仓库的分析文档中；
- 如果将来真正依赖 Pi，应通过明确的包版本、服务边界或独立部署方式接入，而不是把参考源码混入主仓库。

根目录 `.gitignore` 中的 `/pi/` 是这一边界的自动保护措施。

### 2.3 本地文件与敏感数据

以下内容只保留在本地，不进入 Git：

- `node_modules/`、Python 虚拟环境和缓存；
- `.env`、API Key、数据库密码和本地凭据；
- 本地数据库、上传文件、索引文件和运行时状态；
- 构建产物、测试覆盖率、日志和临时文件；
- Agent 本地会话、缓存和实验输出。

## 3. 目录职责

```text
partnerAgent/
├── 需求/
│   ├── README.md
│   ├── v1-*                       # v1 需求、状态机、数据和架构设计
│   └── ...
├── apps/
│   └── partner-agent-backend/     # NestJS 产品业务后端
├── packages/
│   └── contracts/                 # HTTP / WebSocket 共享契约
├── services/                      # 未来 Python AI/RAG
├── infra/                         # 未来 Docker、数据库和部署配置
├── scripts/                       # 未来跨模块项目级脚本
├── pi/                            # 可选本地 Pi 源码，不纳入 Git
├── pi-agent-分析文档.md            # Pi 相关学习和分析结果
├── Pi-Agent-目录说明.md            # Pi 目录结构记录
├── README.md
├── 开发交接说明.md
├── 阶段0-4实施总结.md
├── 项目架构说明.md
├── package.json
├── package-lock.json
└── .gitignore
```

目录只表达职责，不提前创建空目录。前端、Python、基础设施和脚本目录等到对应工作正式开始时再创建。

## 4. 当前服务边界

技术架构基线如下：

```text
前端
  │
  │ HTTP / WebSocket / 文件上传
  ▼
NestJS 业务后端
  │
  ├── 业务 API、认证、权限和任务编排
  ├── Local Core 领域逻辑
  ├── PostgreSQL / 文件存储
  └── Pi Agent Runtime 调用
          │
          └── Python AI/RAG 服务
```

这里的关系是产品架构边界，不表示当前所有服务都已经实现。当前只记录职责方向，不提前固化具体模块、端口或部署方式。

## 5. 开发阶段

### 当前阶段：会话、工具与确认边界准备

当前暂停：

- 业务功能实现；
- 前端初始化；
- Python 项目初始化；
- 未经计划的大范围目录重构；
- Pi 源码修改或二次开发。

当前允许：

- 补充需求和架构文档；
- 完善共享契约；
- 完善内存会话并设计持久化迁移；
- 增加无副作用只读工具；
- 完善数据库与文件存储方案；
- 记录 Pi 源码学习结论；
- 校验仓库边界和忽略规则。

### 恢复开发后的顺序

1. 完成认证、会话所有权和确认中心设计；
2. 完成 PostgreSQL 会话、消息和文件存储设计；
3. 建立工具权限、审计、脱敏与撤销机制；
4. 初始化正式前端和 Python AI/RAG 服务；
5. 再扩展提醒、目标、长期记忆和决策辅助等写能力。

## 6. 何时拆分独立仓库

当前使用一个产品主仓库，不按语言拆分。只有出现以下情况时，才重新评估拆仓库：

- 模块由不同团队独立维护；
- 模块需要独立权限控制；
- 模块有完全独立的发布和部署周期；
- 模块需要作为多个产品复用；
- 主仓库体积或 CI 成本已经影响开发效率。

在这些条件出现前，单一产品仓库更有利于同步需求、接口和跨服务变更。

## 7. 变更规则

- 需求变更先更新 `需求/` 文档，再进入实现；
- 跨模块接口变更必须同步记录契约和影响范围；
- Pi 学习结论进入分析文档，不直接改变产品架构；
- 不因为初始化某种语言就自动新建 Git 仓库；
- 不提交凭据、本地数据、依赖目录和构建产物；
- 恢复开发前，先更新本文件中的阶段状态。
