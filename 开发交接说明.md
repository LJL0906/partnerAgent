# Partner Agent 开发交接说明

> 文档用途：交接当前后端实现、Pi Agent 集成现状和 Monorepo 迁移后的开发边界。
>
> 更新时间：2026 年 9 月 4 日
>
> 交接范围：NestJS 后端、Pi Agent Core 集成、统一模型管理层、Monorepo 组织建议。

---

## 1. 项目定位

Partner Agent 是面向个人长期使用的助手系统，后续会逐步覆盖：

- 对话和个人助手能力；
- 目标、行动、提醒和决策辅助；
- 事实、长期记忆和确认中心；
- RAG 检索与当前处境模型；
- 可追溯、可确认、可撤销的数据闭环。

当前已经完成的是最小可运行的模型对话链路，不代表完整业务闭环已经实现。

---

## 2. 当前仓库结构与边界

当前主仓库目录：

```text
partnerAgent/
├── 需求/                         # 产品需求、状态机和技术设计
├── partner-agent-backend/        # 当前 NestJS 后端
├── pi/                           # Pi 官方源码本地参考仓库，不纳入产品仓库
├── pi-agent-分析文档.md
├── Pi-Agent-目录说明.md
├── 项目架构说明.md
└── README.md
```

### 2.1 `partner-agent-backend/`

这是产品后端，不是 Pi 源码的二次开发目录。当前后端负责：

- HTTP 服务；
- Socket.IO WebSocket 网关；
- Agent 请求编排；
- Provider / Model 选择；
- 将 Pi Agent 内部事件转换为前端事件。

### 2.2 `pi/`

`pi/` 是官方 Pi 项目的本地克隆，用于：

- 阅读源码；
- 核对当前依赖版本的真实 API；
- 参考 Provider、Agent 和事件实现。

不要执行以下操作：

- 不把 `pi/` 复制到产品代码中；
- 不修改 `pi/` 后提交到产品仓库；
- 不把 `pi/` 做成产品仓库的 Git Submodule；
- 不直接依赖 `pi/packages/*` 的源码路径。

产品后端只通过 npm 依赖使用：

```text
@earendil-works/pi-ai@^0.84.4
@earendil-works/pi-agent-core@^0.84.4
```

---

## 3. 当前已经完成的功能

### 3.1 后端基础架构

当前后端是 NestJS 项目，核心模块如下：

```text
partner-agent-backend/src/
├── app.module.ts
├── app.controller.ts
├── app.service.ts
├── main.ts
├── agent/
│   ├── agent.module.ts
│   ├── agent.gateway.ts
│   ├── pi-agent.service.ts
│   └── pi-agent.service.spec.ts
└── model-gateway/
    ├── model-gateway.module.ts
    ├── model-gateway.service.ts
    └── model-gateway.service.spec.ts
```

### 3.2 HTTP 根路由

`AppController` 已经注册到 `AppModule`，根路由可用：

```text
GET /
```

响应：

```text
Hello World!
```

这条路由主要用于基础健康检查和 e2e 验证，后续可以替换为正式 health endpoint，但不要在没有测试覆盖的情况下直接删除。

### 3.3 WebSocket 网关

当前网关：

```text
src/agent/agent.gateway.ts
```

配置：

```text
Socket.IO
默认端口：3000
```

事件：

```text
客户端 → 服务端：chat
客户端 → 服务端：cancel
服务端 → 客户端：agent_event
```

客户端发送：

```json
{
  "message": "你好",
  "sessionId": "可选，目前暂未持久化使用"
}
```

服务端转发的事件格式：

```json
{
  "type": "text_delta",
  "data": "你好",
  "timestamp": 1720000000000
}
```

完成事件：

```json
{
  "type": "done",
  "timestamp": 1720000000000
}
```

错误事件：

```json
{
  "type": "error",
  "data": {
    "message": "错误信息"
  },
  "timestamp": 1720000000000
}
```

当前取消逻辑是连接级别的：一个 Socket 连接同时只建议处理一个对话请求。后续实现多并发会话时，需要把活动 Agent 按 `sessionId` 管理，而不是只按 `client.id` 管理。

---

## 4. Pi 依赖集成方式

### 4.1 `pi-ai`

当前使用 Pi 提供的统一模型集合：

```ts
import { createModels } from '@earendil-works/pi-ai';

const models = createModels();
```

通过 Provider 注册模型：

```ts
import { deepseekProvider } from '@earendil-works/pi-ai/providers/deepseek';

models.setProvider(deepseekProvider());
```

不要自行重复实现 DeepSeek 的 Provider，也不要手写 DeepSeek 的请求格式。当前 Pi 已经内置：

```text
DeepSeek → openai-completions
```

不要依据旧文档把 DeepSeek 改成 `openai-responses`。

### 4.2 `ModelGatewayService`

文件：

```text
partner-agent-backend/src/model-gateway/model-gateway.service.ts
```

职责：

- 创建 `MutableModels`；
- 注册 DeepSeek Provider；
- 根据 API Key 是否存在注册 Anthropic / OpenAI Provider；
- 对外暴露 `getModels()`；
- 在启动日志中输出当前默认 Provider 的模型数量。

当前调用方式：

```ts
const models = this.modelGateway.getModels();
const model = models.getModel(provider, modelId);
```

认证必须沿用 Pi Provider 的认证体系。DeepSeek 内置 Provider 内部使用：

```ts
envApiKeyAuth('DeepSeek API key', ['DEEPSEEK_API_KEY'])
```

不要把 API Key 打印到日志、WebSocket 事件、测试输出或错误信息中。

### 4.3 `PiAgentService`

文件：

```text
partner-agent-backend/src/agent/pi-agent.service.ts
```

当前调用链：

```text
读取 DEFAULT_PROVIDER / DEFAULT_MODEL
        ↓
ModelGatewayService.getModels()
        ↓
models.getModel(provider, modelId)
        ↓
new Agent({ initialState, streamFn })
        ↓
agent.subscribe(...)
        ↓
agent.prompt(message)
        ↓
agent.waitForIdle()
        ↓
转换并输出后端事件
```

Agent 正确使用方式是订阅事件：

```ts
const unsubscribe = agent.subscribe((event) => {
  // 处理 AgentEvent
});

await agent.prompt(message);
await agent.waitForIdle();
unsubscribe();
```

当前版本不应假设 `Agent` 支持：

```ts
for await (const event of agent) {}
```

Pi Agent 的文本增量位于：

```text
AgentEvent.message_update.assistantMessageEvent.type === 'text_delta'
```

文本内容位于：

```text
AgentEvent.message_update.assistantMessageEvent.delta
```

---

## 5. 当前环境配置

文件：

```text
partner-agent-backend/.env
```

配置项：

```text
DEEPSEEK_API_KEY=<本地真实 Key，不提交>
DEFAULT_PROVIDER=deepseek
DEFAULT_MODEL=deepseek-v4-flash
WS_PORT=3000
```

注意：

- `.env` 不得提交到 Git；
- 当前真正用于监听 Nest 服务的是 `PORT`；
- `WS_PORT` 当前没有被 `main.ts` 使用；
- 当前 HTTP 和 WebSocket 共用 Nest 服务端口；
- 如果后续需要拆分 HTTP / WebSocket 端口，应先更新接口和部署设计，再修改代码。

当前 Pi 版本的 DeepSeek 模型目录包含 3 个模型，默认模型 `deepseek-v4-flash` 已经通过真实请求验证。

---

## 6. 验证结果

在 `partner-agent-backend/` 目录执行：

```powershell
npm install
npm run build
npm test
npm run test:e2e
npm run lint
```

当前验证结果：

```text
build       通过
unit tests  3 个测试文件通过
              3 个测试通过
e2e         1 个测试通过
lint        通过
```

另使用临时端口启动服务完成了真实 DeepSeek 验证：

```powershell
$env:PORT='3102'
node dist/main
```

通过 Socket.IO WebSocket 发送真实消息后，收到多个 `text_delta` 和一个 `done`，返回内容为真实 DeepSeek 响应，不再是之前的模拟文本。

---

## 7. Monorepo 迁移建议

目标不是把 Pi 官方仓库并入产品仓库，而是把产品的前端、后端、AI 服务、共享契约和基础设施放在同一个产品 Monorepo 中。

建议目标结构：

```text
partnerAgent/
├── apps/
│   ├── partner-agent-backend/       # NestJS 应用
│   └── partner-agent-frontend/      # 前端应用
├── services/
│   └── partner-agent-ai/            # Python AI / RAG 服务
├── packages/
│   ├── contracts/                   # HTTP / WebSocket / 领域 DTO 契约
│   ├── config/                      # 共享配置和环境变量校验
│   └── tsconfig/                    # 可选，共享 TS 配置
├── infra/
│   ├── docker/
│   ├── postgres/
│   └── deployment/
├── scripts/
├── 需求/
├── pi/                              # 本地参考仓库，继续排除
├── package.json                     # Monorepo 根配置
└── README.md
```

### 7.1 迁移原则

1. **先建立 Workspace，再移动代码。**
2. **迁移过程中保证后端仍能单独启动和测试。**
3. **不要在迁移目录结构的同时扩展业务功能。**
4. **先稳定共享契约，再让前后端依赖契约包。**
5. **Pi 依赖保持在后端应用或专门的 Agent Runtime 包内，不向所有 workspace 暴露。**
6. **敏感配置只能由运行服务读取，不放入共享 packages。**
7. **每次目录迁移都要同步更新构建、测试、lint、Docker 和 CI 路径。**

### 7.2 推荐迁移顺序

#### 第一步：根 Workspace

在根目录建立 Monorepo 包管理方案，推荐先使用 npm workspaces：

```json
{
  "private": true,
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ]
}
```

根目录统一提供：

```text
npm run build
npm run test
npm run lint
```

第一阶段不要急于引入复杂任务编排工具，除非构建规模已经产生明显瓶颈。

#### 第二步：迁移后端

将当前：

```text
partner-agent-backend/
```

迁移为：

```text
apps/partner-agent-backend/
```

迁移时保持以下内容不变：

- `src/agent/` 业务边界；
- `src/model-gateway/` 模型管理边界；
- Socket.IO 事件名称和事件格式；
- `.env` 变量名称；
- `npm run build`、`npm test`、`npm run test:e2e`、`npm run lint` 的语义。

#### 第三步：共享契约

将前后端共同依赖的类型抽到：

```text
packages/contracts/
```

建议先放：

- `ChatRequest`；
- `AgentEvent`；
- `TextDeltaEvent`；
- `AgentDoneEvent`；
- `AgentErrorEvent`；
- WebSocket 事件名称常量。

不要把 Nest Service、Pi Agent 实例或数据库实体放入 contracts 包。

#### 第四步：前端和 AI 服务

前端通过共享契约连接：

```text
partner-agent-frontend
        ↓ HTTP / Socket.IO
partner-agent-backend
        ↓ 内部协议
partner-agent-ai
```

Python AI/RAG 服务后续只负责 RAG、Embedding、文本处理等明确能力，不应绕过 NestJS 直接修改业务数据。

---

## 8. 后续队友开发优先级

### P0：保持当前基线

- 保证 `npm run build` 通过；
- 保证 `npm test` 通过；
- 保证 `npm run test:e2e` 通过；
- 保证真实模型调用不回退到模拟响应；
- 保证 API Key 不进入日志和提交。

### P1：完成 Monorepo 基础迁移

- 建立根 `package.json` workspace；
- 迁移后端到 `apps/partner-agent-backend/`；
- 修正根级脚本；
- 修正 `.gitignore`；
- 保证后端独立启动和根目录启动都能验证；
- 更新 README 和 CI 路径。

### P2：抽取共享契约

- 统一 HTTP DTO；
- 统一 Socket.IO 事件类型；
- 统一错误码和错误结构；
- 前端不再手写事件字符串和事件字段。

### P3：完善 Agent 会话模型

当前 `sessionId` 还未真正使用。后续需要明确：

- 会话存储位置；
- 一个用户是否允许多个会话；
- Agent 实例是否按会话复用；
- 历史消息如何恢复；
- 取消是否按请求、会话还是连接生效；
- 并发请求如何隔离；
- 上下文压缩何时触发。

### P4：接入工具和 Local Core

工具接入前必须先明确：

- 工具权限；
- 是否需要用户确认；
- 工具执行结果是否落库；
- 外部模型能看到哪些字段；
- 敏感信息脱敏边界；
- Agent 是否允许直接修改正式业务对象。

当前设计原则是：RAG 和 Agent 只能提供依据或候选，不能绕过确认中心直接写入正式事实、目标、行动或长期记忆。

---

## 9. 当前已知问题与限制

### 9.1 `WS_PORT` 尚未生效

当前服务使用：

```ts
await app.listen(process.env.PORT ?? 3000);
```

如果队友要拆分 HTTP 和 WebSocket 端口，需要先更新架构设计和客户端配置。

### 9.2 `sessionId` 尚未接入会话存储

Gateway 当前接收 `sessionId`，但只把 `message` 传给 Agent Service。不能把当前实现误认为已经支持持久会话。

### 9.3 活动会话按 Socket 连接管理

当前 `activeSessions` 的 key 是 `client.id`。这适合最小 Demo，不适合多会话、多标签页和并发请求。

### 9.4 Provider 注册策略仍是最小实现

当前必定注册 DeepSeek；Anthropic / OpenAI 只在对应 API Key 存在时注册。后续如果增加数据库模型配置、用户级 Provider 或动态模型目录，需要重新设计配置来源和权限边界。

### 9.5 前端测试客户端仍是静态 HTML

```text
partner-agent-backend/test-client.html
```

它通过 CDN 引入 Socket.IO 客户端，只适合手动验证，不是正式前端实现。

### 9.6 测试中的 Agent Stream 使用测试替身

单元测试不调用真实网络；真实模型调用通过独立手动链路验证。新增测试时应继续避免把真实 API Key 和外部网络作为默认测试依赖。

---

## 10. 接手后的第一天建议

队友接手后建议按以下顺序执行：

```powershell
cd D:\project\ljl\project\partnerAgent

# 1. 查看产品架构和需求边界
Get-Content .\项目架构说明.md
Get-Content .\README.md

# 2. 查看 Pi 依赖分析
Get-Content .\pi-agent-分析文档.md
Get-Content .\Pi-Agent-目录说明.md

# 3. 进入当前后端
cd .\partner-agent-backend
npm install
npm run build
npm test
npm run test:e2e
npm run lint
```

然后再开始 Monorepo 迁移，不要直接修改 Pi 源码或先接入业务工具。

---

## 11. 交接验收清单

### 当前后端基线

- [ ] 根路由 `GET /` 返回 200；
- [ ] Socket.IO 可以连接；
- [ ] `chat` 可以返回 `text_delta`；
- [ ] 真实 DeepSeek 请求可以返回模型内容；
- [ ] 请求结束会返回 `done`；
- [ ] 错误会返回 `error`；
- [ ] `npm run build` 通过；
- [ ] `npm test` 通过；
- [ ] `npm run test:e2e` 通过；
- [ ] `npm run lint` 通过。

### Monorepo 迁移

- [ ] 根 workspace 配置完成；
- [ ] 后端目录迁移完成；
- [ ] 根级脚本完成；
- [ ] 后端独立脚本仍然可用；
- [ ] 共享 contracts 包完成最小版本；
- [ ] 前后端事件格式统一；
- [ ] `pi/` 仍未纳入产品仓库；
- [ ] `.env` 和 API Key 未提交；
- [ ] CI / Docker / README 路径已更新。

---

## 12. 关键原则

1. **先保证运行基线，再扩展业务。**
2. **先找 Pi 当前版本的真实 API，再参考旧文档。**
3. **不要把 Pi 源码当作产品代码维护。**
4. **不要让 WebSocket 事件直接暴露 Pi 内部全部事件结构。**
5. **模型层负责 Provider / Model，业务层负责 Agent 编排。**
6. **共享包只放稳定契约，不放运行时实例。**
7. **真实模型调用必须保留可测试的替身和独立集成验证。**
8. **涉及长期记忆、目标、行动和事实写入时，必须遵守确认和可撤销边界。**
