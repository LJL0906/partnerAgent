# Partner Agent 开发交接说明

> 更新时间：2026 年 9 月 4 日
> 当前状态：Monorepo、共享契约、内存会话和首个只读工具已经落地。

## 1. 项目边界

Partner Agent 是个人长期信息管理与行动辅助产品。当前代码只完成基础 Agent 运行链路，不代表事实、目标、行动、提醒、长期记忆或确认中心已经实现。

必须遵守以下边界：

- `pi/` 仅允许作为本地参考仓库，不纳入本仓库，也不修改后提交；
- 产品代码只通过 npm 包使用 Pi；
- `.env`、API Key、本地数据和构建产物不得提交；
- Pi 内部事件必须转换为产品契约后才能发送给前端；
- RAG 和 Agent 只能提供依据或候选，不能绕过确认中心写入正式事实、目标、行动或长期记忆。

当前检出目录中没有实际 `pi/`，但根 `.gitignore` 已保留 `/pi/` 保护规则。

## 2. 当前目录

```text
partnerAgent/
├── apps/
│   └── partner-agent-backend/     # NestJS、Socket.IO、Pi Agent Runtime
├── packages/
│   └── contracts/                 # 跨端 WebSocket 契约
├── services/                      # 预留 Python AI/RAG 服务
├── 需求/                          # 产品与技术需求
├── package.json                   # npm workspaces 根配置
├── package-lock.json              # Workspace 唯一安装入口锁文件
├── README.md
├── 项目架构说明.md
└── 阶段0-4实施总结.md
```

Workspace 构建顺序是 `packages/* → apps/* → services/*`。contracts 必须先生成 `dist`，后端才能解析其声明文件。

## 3. 开发与验证

要求 Node.js 版本满足当前 NestJS、Vitest 和 Pi 包的约束。统一从仓库根目录安装：

```powershell
cd E:\project\partnerAgent
npm install
npm run build
npm run test
npm run lint
```

后端 e2e 单独执行：

```powershell
cd .\apps\partner-agent-backend
npm run test:e2e
```

启动生产构建：

```powershell
npm run build
cd .\apps\partner-agent-backend
npm run start:prod
```

默认 HTTP 与 Socket.IO 共用端口 `3000`，可用 `PORT` 覆盖。

## 4. 环境变量

本地文件位置：

```text
apps/partner-agent-backend/.env
```

DeepSeek 最小配置：

```dotenv
DEEPSEEK_API_KEY=<本地密钥>
DEFAULT_PROVIDER=deepseek
```

可选配置：

```dotenv
DEFAULT_MODEL=<模型 ID>
OPENAI_API_KEY=<本地密钥>
ANTHROPIC_API_KEY=<本地密钥>
PORT=3000
```

不要记录、打印或提交真实值。`WS_PORT` 当前未被程序读取。

## 5. 共享契约

包名：`@partner-agent/contracts`

主要事件名：

| 方向 | 事件 | 用途 |
| --- | --- | --- |
| 客户端 → 服务端 | `chat` | 向指定会话发送消息 |
| 客户端 → 服务端 | `cancel` | 取消指定会话的当前请求 |
| 客户端 → 服务端 | `resume_session` | 获取指定会话历史 |
| 服务端 → 客户端 | `agent_event` | 统一返回产品级事件 |

发送消息：

```json
{
  "sessionId": "session-123",
  "message": "你好",
  "userId": "可选，当前不作为可信认证依据"
}
```

取消请求：

```json
{
  "sessionId": "session-123"
}
```

服务端事件均包含：

```json
{
  "type": "text_delta",
  "sessionId": "session-123",
  "timestamp": 1788494400000,
  "data": "文本增量"
}
```

当前产品级事件包括 `history`、`text_delta`、`tool_execution_start`、`tool_execution_end`、`done`、`cancelled` 和 `error`。

## 6. 会话模型

`SessionManager` 当前采用进程内存：

- 最多保存 100 个会话；达到上限时拒绝创建新会话；
- 每个会话最多保存最近 100 条 user/assistant 消息；
- Agent 实例按 `sessionId` 创建并复用；
- 单个 `sessionId` 同时只允许一个生成请求；
- 同一 Socket 可以同时管理不同会话；
- 取消按 `sessionId` 生效，不影响其他会话；
- Socket 断开会终止该连接发起的进行中请求，但不会销毁会话；
- 重连后发送 `resume_session` 可恢复历史，并继续复用 Agent 上下文。

限制：服务重启后内存会话会丢失；尚无可信用户身份，因此暂不能执行真正的“每用户会话数”权限控制；当前只有截断策略，没有摘要压缩。

## 7. 工具模型

已接入只读工具：

```text
get_current_time
```

该工具无输入参数，返回服务器 ISO 8601 时间。当前权限规则：

- 无副作用只读工具允许自动执行；
- 工具结果不落库，只进入当前 Agent 会话上下文；
- 模型只能看到工具公开名称、描述、参数 Schema 和筛选后的结果；
- 前端工具事件只包含工具名、调用 ID 与成功状态；
- 不向前端透传 Pi 原始 result、参数、堆栈、密钥或环境变量；
- 任何写工具都必须等待确认中心与审计机制落地。

测试客户端位于：

```text
apps/partner-agent-backend/test-client.html
```

刷新页面后可编辑会话 ID。输入“现在几点？”可验证工具开始、工具完成和最终模型回复。

## 8. 主要代码

```text
apps/partner-agent-backend/src/
├── agent/
│   ├── agent.gateway.ts                 # WebSocket 编排与事件转换
│   ├── pi-agent.service.ts              # Agent 生命周期、历史和工具注册
│   ├── session-manager.service.ts       # 内存会话
│   └── tools/current-time.tool.ts       # 只读时间工具
└── model-gateway/
    └── model-gateway.service.ts         # Provider 与模型目录

packages/contracts/src/
├── events.ts                            # 请求和事件类型
└── index.ts
```

## 9. 当前验证基线

截至本次交接：

```text
npm run build                    通过
npm run test                     通过
contracts Jest                   1 个测试通过
backend Vitest                   5 个测试文件、8 个测试通过
npm run test:e2e                 1 个测试通过
npm run lint                     通过
真实 WebSocket 模型对话          已人工截图验证
```

工具测试覆盖完整 Agent 两轮循环：模型提出工具调用，真实时间工具执行，工具结果回到模型上下文，模型生成最终文本，前端过程事件同步产生。

## 10. 下一步优先级

1. 引入身份认证，建立可信 `userId → sessionId` 所有权校验和每用户限额；
2. 将会话与消息迁移到数据库，明确 Agent 重建和进程重启恢复；
3. 设计基于 token 的上下文窗口与摘要压缩，不再只按消息数截断；
4. 建立工具注册表、风险级别、确认中心、审计记录和结果脱敏器；
5. 仅在上述边界完成后增加事实、目标、行动和长期记忆写工具；
6. 初始化正式前端，并直接消费 `@partner-agent/contracts`，替换静态测试页。

## 11. 提交前检查

- `npm run build`、`npm run test`、`npm run lint` 全部通过；
- 后端 `npm run test:e2e` 通过；
- `.env` 和 API Key 未进入 Git；
- `pi/` 未进入主仓库；
- WebSocket 事件没有直接暴露 Pi 内部结构；
- 新增写能力时必须有确认、授权、审计和撤销设计。
